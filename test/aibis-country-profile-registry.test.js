const assert=require('assert');const Registry=require('../js/aibis/country-profile-registry.js');const Sources=require('../js/aibis/country-profile-sources.js');const Template=require('../js/aibis/country-profile-template.js');
const draft=Template.createEmptyCountryProfile({iso2:'JP',name:'Japan',region:'East Asia'});assert.strictEqual(draft.iso2,'JP');assert.strictEqual(Object.keys(draft.indicators).length,Registry.DIMENSIONS.length);assert.ok(Sources.get('wto-ttd'));assert.strictEqual(Sources.get('wto-ttd').commercialUse,'restricted_check_required');
const indicators={};for(const d of Registry.DIMENSIONS){indicators[d]={value:d==='politicalRisk'?20:70,sourceId:'expert-protocol',sourceUrl:'https://example.invalid',referenceYear:2026,retrievedAt:'2026-08-02',freshness:'current',confidence:75,license:'allowed',direction:d.includes('Risk')||d==='tariffPressure'||d==='culturalDistance'?'higher_is_worse':'higher_is_better'};}
const profile=Registry.createProfile({iso2:'JP',name:'Japan',region:'East Asia',profileVersion:'0.1.0',indicators,provenance:{compiledAt:'2026-08-02',reviewStatus:'draft'}});assert.strictEqual(Registry.validateProfile(profile).valid,true);assert.strictEqual(Registry.profileConfidence(profile),75);assert.strictEqual(Registry.staleness(profile,2026).every(x=>x.status==='current'),true);
const invalid=Template.createEmptyCountryProfile({iso2:'JPN',name:'Japan',region:'East Asia'});const check=Registry.validateProfile(invalid);assert.strictEqual(check.valid,false);assert.ok(check.errors.includes('iso2_invalid'));console.log('aibis-country-profile-registry tests passed');