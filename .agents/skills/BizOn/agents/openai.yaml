interface:
  display_name: "BizOn"
  short_description: "Repo-specific patterns and workflows for BizOn"
  default_prompt: "Use the BizOn repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true